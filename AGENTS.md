# repo instruction

- use `uv run` as python environment

## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues for `LCJD99/eec-sched`. See `docs/agents/issue-tracker.md`.

### Triage labels

The repository uses the five default triage labels. See `docs/agents/triage-labels.md`.

### Domain docs

This repository uses the single-context layout. See `docs/agents/domain.md`.
