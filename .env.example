# Copy this file to .env and replace the placeholder values.
# .env is ignored by git and must never be committed.
EEC_SCHED_REFLECTION_TOKEN=replace-with-reflection-api-token
EEC_SCHED_CODING_TOKEN=replace-with-coding-api-token
